# ===========================================
# ArtCertify EVM (Base + Privy)
# ===========================================

# Privy (REQUIRED for login)
# Get your App ID at https://dashboard.privy.io
VITE_PRIVY_APP_ID=

# Base network (REQUIRED for SBT contract)
# Evita 429: usa un RPC con rate limit più alto (es. Alchemy, QuickNode).
# Esempio Alchemy: https://base-mainnet.g.alchemy.com/v2/TUAO_API_KEY
VITE_BASE_RPC_URL=https://mainnet.base.org
VITE_BASE_EXPLORER_URL=https://basescan.org

# SBT contract (REQUIRED after deploy)
# Deploy with: npx hardhat run scripts/deploy.ts --network base
# Then set VITE_SBT_CONTRACT_ADDRESS to the deployed contract address
VITE_SBT_CONTRACT_ADDRESS=

# Pinata IPFS (REQUIRED for certification uploads)
# Get keys at https://app.pinata.cloud
VITE_PINATA_API_KEY=
VITE_PINATA_API_SECRET=
VITE_PINATA_JWT=
# Optional: custom gateway for reading IPFS (e.g. https://gateway.pinata.cloud/ipfs/)
VITE_PINATA_GATEWAY=

# ===========================================
# Optional: deploy contract (script only)
# ===========================================
# DEPLOYER_PRIVATE_KEY=  # Used by: npx hardhat run scripts/deploy.ts --network base
